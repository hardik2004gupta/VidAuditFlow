[project]
name = "complianceqapipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "azure-identity>=1.25.1",
    "azure-monitor-opentelemetry>=1.8.3",
    "azure-search-documents>=11.6.0",
    "azure-storage-blob>=12.27.1",
    "fastapi>=0.128.0",
    "httpx>=0.28.1",
    "langchain>=1.2.0",
    "langchain-community>=0.4.1",
    "langchain-openai>=1.1.6",
    "langgraph>=1.0.5",
    "langsmith>=0.5.2",
    "opentelemetry-instrumentation-fastapi>=0.60b0",
    "pydantic>=2.12.5",
    "pydantic-settings>=2.12.0",
    "pypdf>=6.5.0",
    "python-dotenv>=1.2.1",
    "tenacity>=9.1.2",
    "uvicorn>=0.40.0",
    "yt-dlp>=2025.1.12",
]

# Removed as part of Phase 2 (backend foundation modernization) dependency
# cleanup -- PROJECT_AUDIT.md / TECHNICAL_DEBT.md TD-8 flagged these as
# declared but never imported anywhere in the codebase:
#   streamlit, redis, sqlalchemy, psycopg2-binary, firecrawl-py, pandas
# `requests` was also removed: `services/video_indexer.py` now uses
# `httpx.AsyncClient` exclusively. `sqlalchemy`/`psycopg2-binary` are
# expected to come back deliberately once the database layer
# (DATABASE_PLAN.md) is actually implemented in a later phase.
